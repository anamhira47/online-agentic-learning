from setuptools import setup, find_packages

setup(
    name='onlinelearning',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # Add your dependencies here
        # 'torch',
    ],
)